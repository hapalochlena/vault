// when you IMPORT a JSON file, it will automatically be treated as a JS OBJECT
import requestSave from './mocks/save-request-api-v0.1.json';
//? not sure actually if the type would be a JS object or remains a json; in any case it's now usable in the code
